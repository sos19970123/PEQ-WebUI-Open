# PEQ WebUI · 部署手册

> 独立项目：`<PEQ-WebUI>`
> 适用场景：在 Windows 上部署 / 迁移 / 接管 EqualizerAPO 调音台（方案 A 后端）。
> 配套：《README.md》《用户操作手册.md》《开发维护文档.md》
> **本文汇总 EqualizerAPO 安装坑、权限、挂载与回滚；日常调音请看用户操作手册。**

---

## 1. 系统要求

| 项 | 要求 |
|---|---|
| 操作系统 | Windows 10/11 x64 |
| Python | 3.10+（本机开发用 3.12；启动脚本默认调用 `py`） |
| 依赖包 | 运行必需：`numpy`；可选精确拟合：`scipy`（缺失自动回退快速引擎） |
| EqualizerAPO | **1.3+ / 1.4.x**（本项目按 1.4.2 验证） |
| 可选数据 | 本地 [AutoEq 4.1.2](https://github.com/jaakkopasanen/AutoEq) 根目录（设备搜索/导入/精确优化器） |
| 权限 | 日常普通用户；**仅首次 ConfigPath 迁移/接管需要一次管理员** |
| 端口 | 仅监听 `127.0.0.1:9100`（可用参数改端口；不对外网开放） |

**不依赖**：Peace GUI、虚拟声卡（Hi-Fi Cable）、Electron 中控或其它 DAW。

---

## 2. 推荐目录布局

```text
<parent-folder>\
├── PEQ-WebUI\                         # 本项目（代码 + 预设 + 曲线 + apo-config）
└── AutoEq-4.1.2\
    └── AutoEq-4.1.2\                  # AutoEq 数据库 + autoeq 包（自行获取）
        ├── autoeq\                    # 精确拟合需要（from autoeq.peq import PEQ）
        ├── results\                   # 预计算 PEQ + CSV
        ├── targets\
        ├── measurements\
        └── webapp\
```

代码默认用**相对路径**找 AutoEq：

```text
<PEQ-WebUI>/../AutoEq-4.1.2/AutoEq-4.1.2
```

若 AutoEq 在别处，启动前设置环境变量：

```powershell
$env:AUTOEQ_ROOT = "D:\path\to\AutoEq-4.1.2"
```

**GitHub 分享注意**：AutoEq 完整库约 **800MB+**，请勿打进本仓库；克隆者自行获取后设置 `AUTOEQ_ROOT`，或放在与项目同级的 `../AutoEq-4.1.2/AutoEq-4.1.2`。没有 AutoEq 时，预设编辑、曲线导入、快速拟合仍可用；「AutoEq 设备库搜索/导入」与「精确拟合」会降级或不可用。

---

## 3. EqualizerAPO 安装（含坑）

### 3.1 安装

1. 从 [EqualizerAPO 官网](https://sourceforge.net/projects/equalizerapo/) 安装（建议 1.4.x）。
2. 安装向导里选择要挂载的设备（代码默认作用域示例为 **Fiio / FiiO Q series**（可在预设与 `DEFAULT_APO_SCOPE` 中按你的设备修改）；换 DAC 需改预设中的 `Device:` 行与 `DEFAULT_APO_SCOPE`）。
3. 安装完成后确认注册表存在：

```text
HKLM\SOFTWARE\EqualizerAPO
  InstallPath
  ConfigPath      ← 关键：APO 引擎读取的配置目录
```

默认通常是：

```text
C:\Program Files\EqualizerAPO\config
```

### 3.2 必须知道的坑

| 坑 | 现象 | 原因与对策 |
|---|---|---|
| **Program Files 写保护** | 普通权限写 `C:\Program Files\EqualizerAPO\config\config.txt` 失败或被虚拟化 | 即使 ACL 给了 Users FullControl，Windows 仍可能拒绝。本项目**不日常写 Program Files**，而是把 `ConfigPath` **一次性迁移到项目内** `apo-config\` |
| **ConfigPath 仍指旧目录** | 页面显示「未托管」；改了预设没声音变化 | 注册表 `ConfigPath` 必须指向 `PEQ-WebUI\apo-config`。执行「首次接管」（§5） |
| **项目搬家后失效** | 移动/重命名 `PEQ-WebUI` 后 APO 读不到配置 | `ConfigPath` 是绝对路径。搬家后需重新 install，或手动改注册表指向新的 `apo-config` |
| **设备未挂载** | `mounted_fiio=false`，EQ 完全不生效 | 安装时未选对设备，或 Windows 更新冲掉挂载。用 **Configurator** 重新勾选目标输出端点（Pre/Post Mix） |
| **Device 作用域不匹配** | 有挂载但本项目 EQ 不生效 | 预设含 `Device: Fiio`（子串匹配 `Fiio (FiiO Q series)`）。换声卡要改预设/默认 scope |
| **Peace 抢配置** | Peace 滑块改动无效果，或反过来本项目失效 | 接管后 `config.txt` 不再 Include `peace.txt`。要回 Peace：执行 uninstall 或恢复备份 |
| **启动脚本不提权** | 首次未迁移就启动，状态一直「未托管」 | `start-webui.ps1` **故意不自动 UAC**。首次必须按 §5 用管理员 PowerShell 接管一次 |
| **APO 与系统独占流** | 某播放器独占 WASAPI 时 EQ 无效 | APO 只作用于共享模式 / 走 Windows 音频引擎的流 |
| **双重处理** | 若同时有其它全局 EQ/宿主且输出走共享模式 | 可能 APO 叠加处理。本项目独立使用时无此问题；与其它全局 EQ 共存时勿双开 |

### 3.3 挂载检测（代码如何判定）

- `installed`：注册表 `HKLM\SOFTWARE\EqualizerAPO` 存在且 `ConfigPath` 可读。
- `managed`：当前生效的 `config.txt` 同时含 `Include: hl-active.txt` 与 Managed 标记（兼容旧版 Managed 标记）。
- `mounted_fiio`：历史字段名；从预设文本中探测是否出现 `Device: Fiio`（与默认 scope 对应，可按设备调整）（近似判断；完整挂载仍以 Configurator / 注册表 Render FxProperties 为准）。

---

## 4. 项目部署步骤

### 4.1 放置项目

将本目录放到固定路径，例如：

```text
<PEQ-WebUI>
```

建议路径中避免频繁改动；含空格/中文一般可用，但排错时更麻烦。

### 4.2 安装 Python 依赖

```powershell
cd <PEQ-WebUI>
py -m pip install numpy
# 可选：精确拟合
py -m pip install scipy
```

### 4.3 配置 AutoEq（可选但推荐）

```powershell
# 默认已在代码中解析为 ..\AutoEq-4.1.2\AutoEq-4.1.2
# 若路径不同：
$env:AUTOEQ_ROOT = "<PEQ-WebUI>/../AutoEq-4.1.2/AutoEq-4.1.2"
```

验证（不启动 Web 服务）：

```powershell
py -c "from autoeq_store import AutoEqStore, DEFAULT_AUTOEQ_ROOT; import os; print(DEFAULT_AUTOEQ_ROOT); print(os.path.isdir(DEFAULT_AUTOEQ_ROOT)); print(len(AutoEqStore().search('HD 800')))"
```

### 4.4 首次启动（可先不接管）

```powershell
cd <PEQ-WebUI>
.\start-webui.ps1
# 或双击 启动PEQ-WebUI.cmd
```

打开浏览器：`http://127.0.0.1:9100`

> 未接管时页面可能提示「配置未托管」——预设仍可编辑，但**系统声音不会变**。继续 §5。

---

## 5. 首次接管（唯一需要管理员的一步）

目标：把 APO 的配置目录从 Program Files 迁到项目内，并让 `config.txt` Include 本项目的 `hl-active.txt`。

### 5.1 推荐命令

以**管理员身份**打开 PowerShell：

```powershell
cd <PEQ-WebUI>
# 确保 auto-install 开关允许（默认文件不存在时 auto_install 可能直接返回，见代码）
py -c "from apo_backend import ApoBackend; a=ApoBackend(); a.set_auto_install_enabled(True); print(a.install(by='admin'))"
```

或：

```powershell
py -c "from apo_backend import ApoBackend; ApoBackend().auto_install()"
```

（`auto_install` 需要 `apo-presets/auto-install.json` 中 `enabled: true`。）

### 5.2 接管时实际做了什么

1. 创建/使用 `<项目>\apo-config\`
2. 若原 `ConfigPath` 下有 `config.txt` 则拷贝一份到项目内；否则写入最小 Managed 配置
3. 备份：`apo-config\config.txt.bak-headphonelab`（若能读到原配置；已存在则保留更早备份）
4. 写入 Managed 标记 + `Include: hl-active.txt`
5. 确保 `hl-active.txt` 存在（不存在则直通注释头）
6. 写注册表 `HKLM\SOFTWARE\EqualizerAPO\ConfigPath = <项目>\apo-config`
7. 记录 `apo-presets\migration.json`（含 `original_config_dir`，供卸载回滚）

### 5.3 接管后验证

1. 刷新 WebUI，状态应为：**APO 已安装 / 配置已托管**（设备挂载视本机而定）。
2. 查看注册表 ConfigPath 是否指向本项目 `apo-config`。
3. 在页面激活一个带明显增益的预设，播放系统音频，确认有变化。
4. 再切回 `pure`（直通），确认声音恢复。

---

## 6. 日常启动与停止

| 操作 | 方式 |
|---|---|
| 启动 | 双击 `启动PEQ-WebUI.cmd` 或 `.\start-webui.ps1` |
| 停止 | 页面顶部「停止服务」；或结束占用 9100 的进程 |
| 重启 | 页面「重启服务」；或 stop 后再 start |
| 单例 | 重复启动会检测端口/锁文件并提示已运行，避免多开 |

日志：项目根目录 `peq-webui.log` / `peq-webui.err.log`。

---

## 7. 卸载 / 回滚

### 7.1 只撤销本项目对 config 的接管

```powershell
cd <PEQ-WebUI>
py -c "from apo_backend import ApoBackend; print(ApoBackend().uninstall(by='admin'))"
```

会：

- 从当前 `config.txt` 去掉 Managed 标记与 `Include: hl-active.txt`
- 若 `migration.json` 记录了原目录，则尝试把注册表 `ConfigPath` 改回（可能再次 UAC）

### 7.2 手工回滚

1. 用备份：`apo-config\config.txt.bak-headphonelab` 或你自己的备份覆盖 `config.txt`
2. 注册表 `ConfigPath` 改回 `C:\Program Files\EqualizerAPO\config`（管理员）
3. 重启相关音频应用或注销登录，使 APO 重新加载

### 7.3 完全移除 EqualizerAPO

控制面板卸载 EqualizerAPO；本项目目录可整夹删除（预设/曲线都在项目内）。

---

## 8. 故障排查速查

| 症状 | 检查顺序 |
|---|---|
| 页面打不开 | 是否已启动；`netstat -ano \| findstr :9100`；看 err.log |
| 一直「未托管」 | §5 管理员接管；注册表 ConfigPath；config.txt 是否含 Include |
| 托管了但声音无变化 | 设备是否挂载 APO；预设 `Device:` 是否匹配；是否激活了 pure；播放器是否独占 |
| 搜索 AutoEq 无结果 | `AUTOEQ_ROOT` 是否存在；`results/` 是否完整；网络无关（纯本地） |
| 精确拟合不可用 | 是否安装 scipy；AutoEq 根下是否有 `autoeq/peq.py` |
| 写文件权限错误 | 项目目录是否只读；杀毒软件锁定；不要把项目放在受保护的 Program Files |
| 改了预设要很久才生效 | 正常应亚秒级；检查是否写到了未激活的预设文件而非 `hl-active.txt` |
| 多实例参数乱 | 确保只有一个 9100 实例；锁文件 `peq-webui.lock` |

---

## 9. 与其它软件的边界

| 软件 | 关系 |
|---|---|
| **Peace GUI** | 与本项目共用 EqualizerAPO 配置；接管后 Peace 只读分析。要同时用 Peace，先 uninstall |
| **其它全局音频宿主** | 本项目**不连接**任何 DAW。若其它宿主以共享模式输出到已挂载 APO 的设备，可能双重 EQ——勿对同一设备同时开两套全局 EQ |
| **中控 / 悬浮窗** | 可选：按《开发维护文档.md》§7.9 文件契约离线激活；本项目不依赖它们 |

---

## 10. 验收清单（部署完成后自检）

- [ ] `py -c "import apo_backend, mixer_web, autoeq_store, autofit"` 无报错
- [ ] 默认 AutoEq 路径存在，或已设置 `AUTOEQ_ROOT`
- [ ] WebUI `http://127.0.0.1:9100` 可打开
- [ ] `/api/status`：`installed=true`；`managed=true`（接管后）
- [ ] 激活测试预设后系统声音可感知变化；切 `pure` 恢复
- [ ] 卸载回滚路径可按 §7 执行（建议在非日常环境演练一次）

---

## 11. 路径与环境变量一览

| 变量/路径 | 含义 | 默认 |
|---|---|---|
| 项目根 | 代码与数据 | `<PEQ-WebUI>` |
| `APO_PRESET_DIR` | 预设仓库 | `<项目>/apo-presets` |
| `APO_CONFIG_DIR` | APO 配置目录 | `<项目>/apo-config`（迁移后） |
| `AUTOEQ_ROOT` | AutoEq 根 | `<项目>/../AutoEq-4.1.2/AutoEq-4.1.2` |
| `MIXER_STATE_FILE` | 状态文件 | `<项目>/state.json` |
| 注册表 | `HKLM\SOFTWARE\EqualizerAPO\ConfigPath` | 接管后指向项目 `apo-config` |

---

*文档说明：事实来自 Equalizer APO 实测与 `apo_backend.py` 实现；仅保留部署所需边界说明。*
