# PEQ WebUI 开发维护文档

> 适用开发者：后续需要维护 / 扩展 EqualizerAPO WebUI 的工程师
> 配套文档：《部署手册.md》《用户操作手册.md》《webui操作手册.html》
> 独立项目路径：`F:\MIMO-Space\PEQ-WebUI`（AutoEq 数据库默认在同级 `F:\MIMO-Space\AutoEq-4.1.2\AutoEq-4.1.2`）

---

## 1. 项目定位

PEQ WebUI 是 EqualizerAPO 的调音台 Web 前端 + 后端。

- 只负责 APO 预设的编辑、保存、激活
- 不连接 Reaper 8080
- 不切换系统默认输出设备
- 不控制 Reaper 引擎
- 端口铁律：默认只监听 `127.0.0.1:9100`

---

## 2. 目录结构

```text
PEQ-WebUI/
├── mixer_web.py              # HTTP 后端主服务（REST + SSE + 静态托管）
├── apo_backend.py            # APO 配置渲染 / 原子写 / 激活 / 接管 / 迁移 / 状态
├── autoeq_store.py           # 本地 AutoEq 源码索引 + 读取 raw/PEQ/target
├── autoeq_engine.py          # 可选精确拟合（AutoEq PEQ + scipy）
├── state_store.py            # per-preset applied / history 本地状态
├── curve_store.py            # 设备/目标曲线库（热加载 + 导入）
├── curve_align.py / curve_adjust.py / rig_guard.py
├── eq_parser.py              # APO/AutoEq/CSV/JSON → bands；parse_file
├── autofit.py                # 纯 numpy 自动拟合
├── start-webui.ps1           # 普通权限后台启动（不自动 UAC、不自动接管）
├── 启动PEQ-WebUI.cmd         # 用户双击启动入口
├── README.md                 # 快速开始
├── 部署手册.md               # EqualizerAPO 安装/权限/接管/回滚
├── 用户操作手册.md           # 给最终用户
├── 开发维护文档.md           # 本文档
├── webui操作手册.html        # HTML 版操作说明
├── apo-presets/              # 预设仓库
│   ├── <id>.txt              # 预设实际 APO 文本
│   ├── presets.json          # 预设清单缓存
│   ├── apo-state.json        # 激活状态缓存
│   ├── auto-install.json     # 自动接管开关
│   └── migration.json        # ConfigPath 迁移记录
├── apo-config/               # 迁移后的 APO 配置目录
│   ├── config.txt            # APO 总配置（Include: hl-active.txt）
│   └── hl-active.txt         # 当前激活预设/直通文件
├── curves/                   # 设备/目标曲线 JSON
│   ├── devices/
│   └── targets/
├── measurements/             # 历史测量 CSV / 一次性分析脚本（数据源）
├── webui/                    # 纯静态前端
│   ├── index.html
│   ├── styles.css
│   ├── app.js
│   └── manual.js
└── tools/
    ├── seed_presets.py
    ├── import_autoeq_targets.py
    ├── import_targets.py
    ├── import_fd02.py
    ├── import_xlm.py
    ├── import_oratory_curves.py
    └── import_measurement_variants.py   # 从本地 AutoEq 库导入设备频响多 rig 版本（含 rig 元数据）
```

可选外部依赖（不放进本仓库时）：

```text
F:\MIMO-Space\AutoEq-4.1.2\AutoEq-4.1.2\   # AutoEq 4.1.2 数据与 autoeq 包
```

环境变量覆盖：`APO_PRESET_DIR` / `APO_CONFIG_DIR` / `AUTOEQ_ROOT` / `MIXER_STATE_FILE`。

---

## 3. 核心数据流

### 3.1 保存

```text
前端 draft bands
  → POST /api/eq/apply {preset, bands, preamp_db, activate}
  → apo.update_preset()
  → render_preset() 渲染 APO 文本
  → 原子写 apo-presets/<id>.txt
  → 若是激活预设或 activate=true：
      → 原子写 F:\MIMO-Space\PEQ-WebUI\apo-config\hl-active.txt（迁移后）
  → 更新 presets.json / apo-state.json / state.json
```

### 3.2 读取

```text
GET /api/presets
  → 扫描 apo-presets/*.txt
  → 读取 # name / # device / # target / bands / preamp
  → 计算 active（与 hl-active.txt 哈希对账）
  → 返回前端

GET /api/eq/bands?preset=id
  → 读取预设文件 + state.json applied / history
  → 返回 bands / preamp / snapshot
```

### 3.3 AutoEq 导入

```text
GET /api/autoeq/search?q=...
  → autoeq.build_index() 扫描本地 AutoEq results/
  → 返回设备 id/name/source/form

POST /api/autoeq/import {device_id, preset?}
  → 读取 ParametricEQ.txt → bands/preamp
  → 读取 CSV raw 列 → 导入为设备曲线（携带 measurement_meta：measured_device / variant / rig / rig_family / rig_standard / rig_note / url，见 autoeq_store.measurement_meta()）
  → 读取 CSV target 列（若有）→ 导入为目标曲线
  → 更新预设 device_id/target_id/bands
```

### 3.4 曲线导入

```text
POST /api/curves/import {curve_type, name, text, kind?}
  → curve_store.parse_points_text() 解析 CSV / GraphicEQ
  → 写入 curves/<devices|targets>/<slug>.json
  → 前端刷新设备/目标列表
```

### 3.5 APO 生效

```text
EqualizerAPO
  → 读取 config.txt
  → Include: hl-active.txt
  → 监测 hl-active.txt 变化
  → 实时重载
```

---

## 4. 后端模块说明

### 4.1 `apo_backend.py`

职责：

- `render_preset()`：bands → APO 文本（含 `# name` / `# device` / `# target` 元数据）
- 预设 CRUD：
  - `create_preset()`
  - `update_preset()`
  - `delete_preset()`
  - `list_presets()`
  - `get_preset()`
  - 重命名由 `update_preset(..., name=...)` 实现
- 激活：
  - `activate(preset_id)`
  - `active_preset_id()`（哈希对账）
- 接管 / 回退 / 迁移：
  - `install()`
  - `uninstall()`
  - `auto_install()`
  - `migrate_to_user_config_dir()`
  - `_set_config_path_registry()`
- 权限：
  - `_ensure_config_writable()`
  - `_probe_config_writable()`
  - `_grant_config_dir_write_permission()`
- 状态：
  - `status()`
  - `is_installed()`
  - `is_managed()`
  - `is_mounted_fiio()`
  - `is_elevated()`

### 4.2 `mixer_web.py`

- HTTP 服务，默认端口 9100
- 路由 REST API
- SSE 事件广播
- 静态托管 `webui/`
- 注意：`main()` 不调用 `auto_install()`；自动接管只会在显式执行 `ApoBackend().auto_install()` 时发生

### 4.3 `autoeq_store.py`

- 默认 AutoEq 根目录：`F:\MIMO-Space\AutoEq-4.1.2\AutoEq-4.1.2`
- 可用环境变量 `AUTOEQ_ROOT` 覆盖
- `build_index()`：扫描 `results/<source>/<form>/<device>/*.txt + *.csv`
- `search(q)`：按设备名/source 搜索
- `read_eq()`：读取 ParametricEQ 并调用 `eq_parser.parse_eq()`
- `read_device_csv_points()` / `read_device_raw_points()` / `read_device_target_points()`：读 CSV 的 raw/smoothed/target

### 4.4 `state_store.py`

按 `preset_id` 保存：

```json
{
  "applied": {
    "preset-id": {
      "bands": [],
      "preamp_db": 0,
      "snapshot_id": null
    }
  },
  "history": {
    "preset-id": []
  }
}
```

用于撤销“上次保存”。历史上限 20 条。

### 4.5 `curve_store.py`

- 曲线目录 `curves/devices/*.json`、`curves/targets/*.json`
- 热加载，修改 JSON 后无需重启
- 内置设备/目标元数据；同 id 文件可覆盖
- **JSON 中所有附加字段原样透出**（`variant` / `rig` / `rig_family` / `rig_standard` / `rig_note` / `url` / `default`），
  前端下拉框据此渲染 rig 标签与策略说明（见 7.10）
- `import_curve()`：写 JSON，支持 CSV / `GraphicEQ:` 文本
- `parse_points_text()`：`freq,db`、`freq<tab>db`、CSV 表头、EqualizerAPO GraphicEQ

### 4.6 `eq_parser.py`

支持：

- EqualizerAPO / AutoEq 文本
- CSV / 行式
- JSON 数组 / 对象
- `parse_eq_file(path)`：直接读取本地文件，兼容 UTF-8 / GBK

统一输出：

```json
{
  "bands": [
    {"type": "PK", "freq": 105, "gain_db": -3.5, "q": 2.0, "enabled": true}
  ],
  "preamp_db": -3.6,
  "warnings": []
}
```

### 4.7 `autofit.py`

- 纯 numpy，无 scipy（方案 A v3：低频保底锚点 + 峰值贪心布点 + 坐标下降精修）
- 自动拟合成目标曲线
- 返回 `bands` + `rms_db` + 建议 `preamp_db` + `warnings`

### 4.8 `autoeq_engine.py`

- 可选精确模式（方案 B1）：接入本地 AutoEq 4.1.2 `autoeq.peq.PEQ` SLSQP 联合优化
- 依赖 scipy；缺失时后端自动回退 `autofit.autofit()` 快速模式，并在 `warnings` 中提示
- 安装 scipy 后重启后端即自动启用 `PRECISE_AVAILABLE=True`，无需改代码
- 支持 `auto_shelf` 与 AutoEq 默认 10kHz 以上均值能量处理

---

## 5. 前端说明

### 5.1 技术栈

- 原生 HTML / CSS / JavaScript
- 无框架、无构建步骤
- 通过 REST + SSE 与后端通信

### 5.2 主要功能模块

| 模块 | 位置 / 职责 |
|---|---|
| 状态加载 | `loadStatus()` / `loadPresets()` / `loadDevices()` / `loadTargets()` |
| 设备下拉（rig 多版本） | `renderDeviceSelect()` / `renderNewPresetDeviceSelect()`：按设备基础名分组，选项 = 数据源 + rig 短标签 + ★默认，`title` 悬浮显示完整策略；当前版本显示在 `#device-curve-info` |
| rig 策略与跨 rig 警告 | `deviceRigFamilyKey()` / `targetRigFamilyKey()` / `updateRigWarning()`；页面 `测量 rig 策略标准` 面板（index.html `<details class="rig-policy">`） |
| 预设卡片 | `renderPresets()`（单击选中、双击激活、重命名、删除、拖拽排序；拖拽经 `/api/preset/order` 落盘，localStorage 仅兜底） |
| 切换确认 | `promptSaveBeforeSwitch()` |
| 曲线画布 | `renderChart()` / RBJ Biquad 预览 |
| 图表编辑 | `onCanvasDblClick()` / `onCanvasMouseDown()` / `onCanvasWheel()` |
| EQ 表格 | `renderBands()` |
| 手动滑块 | `renderManualEq()` |
| 导入/拟合 | `onParse()` / `onAutofit()` / `adoptPreview()` |
| 服务生命周期 | `onServiceRestart()` / `onServiceStop()`：调用 `/api/admin/restart|stop`，重启时轮询 `/api/status` 恢复后自动刷新；停止/重启均先 confirmDialog |
| AutoEq 库 | `onAutoEqSearch()` / `onAutoEqImport()` / `onAutoEqImportTarget()` |
| 曲线导入 | `openCurveImportModal()` / `confirmCurveImport()` |
| 自动拟合开关 | `auto-fit-switch` → `runAutoFitAndAdopt()` |
| 保存/撤销 | `onApply()` / `onRevert()` / `onDiscard()` |
| 手动保存 | `onApply()`；编辑只更新草稿，不再自动保存/实时部署 |

### 5.3 曲线图例

| 曲线 | 变量 | 颜色 |
|---|---|---|
| Raw 原始 | `deviceCurve.points` | 灰色实线 |
| Target 目标 | `targetCurve.points` | 浅蓝虚线 |
| Equalizer 均衡器 | `compositeLine(draftBands, [])` | 橙色实线 |
| Equalized 均衡后 | `compositeLine(draftBands, deviceCurve)` | 蓝色实线 |
| 已保存 | `compositeLine(appliedBands, deviceCurve)` | 绿色实线 |
| 草稿 | `compositeLine(draftBands, deviceCurve)` | 橙色实线 |
| 预览 | `previewBands` | 紫色虚线 |

---

## 6. API 契约

### 6.1 状态 / 预设

| Method | Path | 说明 |
|---|---|---|
| GET | `/api/status` | 后端 + APO 状态（含 `active_preset_name`、`elevated`） |
| GET | `/api/presets` | 预设列表及激活状态（含 `target_id`） |
| POST | `/api/preset/add` | 新增预设 `{name, device_id?}` |
| POST | `/api/preset/rename` | 重命名预设 `{id, name}` |
| POST | `/api/preset/del` | 删除预设 |
| POST | `/api/preset/order` | 保存拖拽排序 `{order: [id, ...]}` |
| PUT | `/api/preset/device` | 修改预设绑定设备（自动移动到新设备块） |
| PUT | `/api/preset/target` | 修改预设目标曲线 |
| POST | `/api/preset/activate` | 激活 `{id}` 或直通 `{id: null}` |

### 6.2 EQ 编辑

| Method | Path | 说明 |
|---|---|---|
| GET | `/api/eq/bands?preset=id` | 读取 applied bands |
| POST | `/api/eq/apply` | 保存并可选激活 |
| POST | `/api/eq/revert` | 撤销上次保存 |
| POST | `/api/eq/parse` | 解析 EQ 文本 |
| POST | `/api/eq/parse_file` | 直接读取并解析本地 EQ 文件 |
| POST | `/api/eq/autofit` | 自动拟合 |

### 6.3 配置 / 管理

| Method | Path | 说明 |
|---|---|---|
| GET | `/api/preset/config?id=id` | 返回渲染后的 APO 文本 |
| POST | `/api/admin/install` | APO 接管 / 迁移 |
| POST | `/api/admin/uninstall` | APO 回退 |
| POST | `/api/admin/restart-elevated` | 管理员模式重启后端（当前 UI 未渲染按钮，外部可调用） |
| POST | `/api/admin/stop` | **停止服务**：响应后约 1 秒延迟退出进程 |
| POST | `/api/admin/restart` | **重启服务**：后台拉起独立新进程（`sys.executable mixer_web.py --port <port> --wait-lock`，日志重定向到 peq-webui.log/.err.log），旧进程延迟退出；新进程带单例锁等待 + 端口绑定重试 |
| POST | `/api/admin/maintenance` | 方案 A 废弃接口，保留为 no-op |

### 6.4 曲线 / AutoEq

| Method | Path | 说明 |
|---|---|---|
| GET | `/api/devices` | 设备列表 |
| GET | `/api/targets` | 目标曲线列表 |
| GET | `/api/curves/device/:id` | 设备曲线 |
| GET | `/api/curves/target/:id` | 目标曲线 |
| POST | `/api/curves/import` | 导入设备/目标曲线（目标曲线可传 `category`: `common` / `headphone`） |
| GET | `/api/autoeq/search?q=` | 搜索本地 AutoEq 结果 |
| POST | `/api/autoeq/import` | 导入 AutoEq 设备曲线 + PEQ + 目标到预设 |
| POST | `/api/autoeq/import_target` | 将 AutoEq 原始频响导入为目标曲线 |

### 6.5 SSE

`GET /api/events`

事件：

- `preset_activated`
- `preset_changed`
- `eq_applied`
- `apo_state`
- `status`（曲线导入等状态变化时广播）

---

## 7. 文件契约

### 7.1 `apo-presets/<id>.txt`

APO 原生配置文本：

```text
# name: DT900 Pro X · Harman 校准
# device: dt900prox
# target: auto-target
# generated: 2026-08-23T10:00:00 by mixer_web
Device: Fiio
Preamp: -3.6 dB
Filter 1: ON PK Fc 105 Hz Gain -3.5 dB Q 2.00
```

元数据 `# name` / `# device` / `# target` 会被后端解析并写入 `presets.json`。

### 7.2 `hl-active.txt`

当前激活预设的完整内容拷贝。空文件或仅注释文件 = 直通。

### 7.3 `presets.json`

预设清单缓存，后端每次增删改后重写。**数组顺序即展示顺序**，且每个对象含 `sort_order` 字段。典型字段：

```json
[
  {
    "id": "dt900prox-harman",
    "name": "DT900 Pro X · Harman 校准",
    "device_id": "dt900prox",
    "target_id": "auto-target",
    "band_count": 10,
    "sort_order": 0,
    "mtime": 1780000000.0
  }
]
```

排序规则：`pure` 置顶 + 同设备块归组；落盘顺序由 `preset-order.json` 与设备分组自动归一化共同决定。

### 7.3.1 `preset-order.json`

用户拖拽后的完整预设顺序，数组元素为预设 id。WebUI 后端原子写，外部只读：

```json
[
  "pure",
  "r70x-harman",
  "r70x-r70xa拟合",
  "dt900prox-harman"
]
```

新建预设会自动插入同设备块；修改设备绑定时也会移动到新设备块。`pure` 固定置顶且不参与同设备归组。缺失时 `list_presets()` 按 `device_id` 分组兜底。

### 7.4 `apo-state.json`

```json
{
  "active_id": "dt900prox-harman",
  "updated_at": "2026-08-22T22:00:00",
  "by": "backend"
}
```

### 7.5 `state.json`

WebUI 本地编辑状态：

```json
{
  "applied": {},
  "history": {}
}
```

### 7.6 `auto-install.json`

```json
{
  "enabled": true
}
```

- `true`：当显式调用 `ApoBackend().auto_install()` 时允许自动接管
- `false`：卸载回退后自动关闭，避免后续自动接管
- 注意：当前 `mixer_web.py` 启动时不会自动调用 `auto_install()`

### 7.7 `migration.json`

```json
{
  "migrated": true,
  "original_config_dir": "C:\\Program Files\\EqualizerAPO\\config"
}
```

记录迁移前的原始 `ConfigPath`，用于“卸载回退”时恢复。

### 7.8 曲线 JSON

`curves/devices/*.json` / `curves/targets/*.json` 结构：

```json
{
  "id": "my-headphone",
  "name": "我的耳机",
  "kind": "headphone",
  "source": "imported",
  "points": [[20, 0.0], [100, -1.0], [1000, 0.0]]
}
```

### 7.10 设备曲线多版本与测量 rig 元数据

同一台设备允许在 `curves/devices/` 下存放**多个测量版本**（不同测量 rig 的原始频响）。
文件名即设备 id：`<device>-<source>.json`；**不带 `-<source>` 后缀的 id 为默认/推荐版本**
（oratory1990），并与历史预设 `device_id` 保持兼容（如 `dt900prox`、`r70x`）。

设备曲线 JSON 除 7.8 字段外，可附带以下 rig 元数据（前端下拉框据此展示标签与策略说明）：

```json
{
  "id": "r70x-rtings",
  "name": "铁三角 ATH-R70X",
  "kind": "headphone",
  "source": "rtings",
  "variant": "RTINGS",
  "rig": "HMS II.3",
  "rig_family": "HATS",
  "rig_standard": "HEAD acoustics HMS II.3 头身模拟器（HATS，IEC 60268-7 耳廓 + HRTF 补偿）",
  "rig_note": "……补充说明……",
  "url": "https://www.rtings.com/headphones/tests/sound-quality",
  "default": false,
  "points": [[20, -6.82], [20.2, -6.75]]
}
```

字段约定：

| 字段 | 含义 |
|---|---|
| `variant` | 数据源显示名（如 `oratory1990` / `RTINGS` / `InnerFidelity` / `Kuulokenurkka` / `Super* Review` / `Auriculares Argentina`） |
| `rig` | rig 短标签，取自 AutoEq `measurements/<source>/name_index.tsv` 的 rig 列（如 `GRAS 45BC-10`、`HMS II.3`、`KB501x + 711`、`KB006x + 711`） |
| `rig_family` | rig 族（`KEMAR HATS` / `HATS` / `711 coupler` / `5128` 等），前端用它做跨 rig 兼容判断 |
| `rig_standard` | 测量标准一句话（人工耳/人头型号 + 规范） |
| `rig_note` | 数据来源与注意事项（如“RTINGS 现行官方 rig 已是 5128-B”） |
| `url` | 数据来源链接 |
| `default` | 是否默认版本（oratory1990 版本置 `true`） |

> **AutoEq 导入的曲线**（`POST /api/autoeq/import`、`/api/autoeq/import_target`）会自动附带
> `measured_device`（被测耳机名）、`measurement_source`、`variant`、`rig`、`rig_family`、
> `rig_standard`、`rig_note`、`url`（`autoeq_store.measurement_meta()`，优先读
> `measurements/<source>/name_index.tsv` 的 rig 列，回退到内置 `RIG_FALLBACK` 表）。
> 目标曲线下拉框据此显示“设备名 · 测试源 / rig”，设备下拉框同理。

当前收录的版本矩阵（生成于 2026-08，AutoEq 4.1.2 测量库）：

| 设备 | 版本 id | 数据源 | rig（AutoEq 记录） |
|---|---|---|---|
| DT 900 Pro X | `dt900prox` ★ | oratory1990 | GRAS 45BC-10（KEMAR HATS） |
| | `dt900prox-rtings` | RTINGS | HMS II.3（旧版原始图；官方现用 5128-B） |
| | `dt900prox-superreview` | Super* Review | KB006x + 711 |
| | `dt900prox-kuulokenurkka` | Kuulokenurkka | KB501x + 711 |
| ATH-R70X | `r70x` ★ | oratory1990 | GRAS 45BC-10（KEMAR HATS） |
| | `r70x-rtings` | RTINGS | HMS II.3（旧版原始图；官方现用 5128-B） |
| | `r70x-innerfidelity` | InnerFidelity | HMS II.3（HRTF 补偿，5 次佩戴平均） |
| | `r70x-kuulokenurkka` | Kuulokenurkka | KB501x + 711 |
| | `r70x-auriculares-argentina` | Auriculares Argentina | KB501x + 711 |

> crinacle 对 R70X 也有 `EARS + 711`（旧）与 `GRAS 43AG-7`（新）两套原始数据，但其原始数据未公开下载，AutoEq 本地库仅有索引，故未收录。

### 7.9 外部激活契约（可选对接）

本项目独立运行；若另有中控/悬浮窗需要在后端离线时切换预设，可按下列文件契约直读直写（与 WebUI 后端兼容）：

```text
1. 读 %APO_PRESET_DIR%\presets.json（数组顺序即展示顺序）
2. 读 %APO_PRESET_DIR%\preset-order.json（如需自行扫目录重建时归组/排序）
3. 读 %APO_PRESET_DIR%\<id>.txt
4. 把内容原子写入 <项目>\apo-config\hl-active.txt
5. 更新 apo-state.json
```

- 预设目录：`F:\MIMO-Space\PEQ-WebUI\apo-presets\`
- 激活目录：`F:\MIMO-Space\PEQ-WebUI\apo-config\`
- 直通：写入空或仅注释的 `hl-active.txt`
- 全程不需要 WebUI 后端运行，也不需要 UAC
- 预设**内容**仍应只由 WebUI 后端创作/修改；外部只做激活/直通

---

## 8. 权限与 UAC

### 8.1 为什么需要管理员

`C:\Program Files\EqualizerAPO\config` 是 Program Files 保护目录，普通权限进程可能被拒绝写入；初始化时通过 `install()` / `auto_install()` 一次性迁移到 `F:\MIMO-Space\PEQ-WebUI\apo-config`，并把注册表 `ConfigPath` 指向项目内目录。迁移后日常写入无需管理员权限。

注意：

- `start-webui.ps1` **不会自动 UAC 提权**，它只以当前用户权限后台启动 `mixer_web.py`
- 如果状态显示“未托管 / 未接管”，需要在管理员 PowerShell 中执行一次：
  ```python
  from apo_backend import ApoBackend
  ApoBackend().auto_install()
  ```
- API `POST /api/admin/install` 内部也会尝试触发迁移/写权限，必要时弹 UAC

### 8.2 自动接管

`ApoBackend.auto_install()` 的语义：

- `auto-install.json` 默认允许自动接管
- 如果已托管，则只确保 `hl-active.txt` 存在
- `uninstall()` 会把 `auto-install.json` 设为 `false`
- 当前 WebUI 页面没有渲染“初始化接管 APO”按钮；需要接管时通过管理员命令或 API 完成

---

## 9. 保存与生效机制

- 前端编辑只更新草稿，**不自动保存、不实时部署**
- 用户点击“保存到预设”或勾选“保存后立即激活”后调用 `/api/eq/apply`
- 激活时后端写入 `hl-active.txt`
- EqualizerAPO 检测文件变化并重载

注意：

- 如果编辑未激活预设，只写 `apo-presets/<id>.txt`
- 不激活时不会影响当前声音
- 激活态对账以 `hl-active.txt` 内容哈希为准，而不是 `apo-state.json`

---

## 10. 开发与验证

### 10.1 语法检查

```powershell
cd F:\MIMO-Space\PEQ-WebUI
py -m py_compile apo_backend.py mixer_web.py autoeq_store.py state_store.py curve_store.py eq_parser.py autofit.py
node --check webui\app.js
```

### 10.2 本地隔离测试

用临时目录避免污染真实 APO：

```powershell
$env:APO_PRESET_DIR = "$env:TEMP\peq-presets-test"
$env:APO_CONFIG_DIR = "$env:TEMP\peq-config-test"
$env:MIXER_STATE_FILE = "$env:TEMP\peq-state-test.json"
$env:AUTOEQ_ROOT = "F:\MIMO-Space\AutoEq-4.1.2\AutoEq-4.1.2"
py mixer_web.py --port 9101
```

### 10.3 种子预设 / 曲线导入工具

```powershell
py tools\seed_presets.py
py tools\import_autoeq_targets.py
py tools\import_targets.py
py tools\import_measurement_variants.py   # 设备频响多 rig 版本导入（见 7.10 / 11.6）
```

### 10.4 API 冒烟测试

启动后直接请求：

```powershell
Invoke-RestMethod http://127.0.0.1:9100/api/status
Invoke-RestMethod http://127.0.0.1:9100/api/presets
Invoke-RestMethod http://127.0.0.1:9100/api/autoeq/search?q=AKG
```

---

## 11. 扩展指南

### 11.1 新增一个预设生成逻辑

1. 在 `apo_backend.py` 增加生成函数
2. 调用 `render_preset()` 生成文本
3. 写入 `apo-presets/<id>.txt`
4. 调用 `list_presets()` 刷新缓存

### 11.2 新增设备/目标曲线

- 直接写 JSON 到 `curves/devices/` 或 `curves/targets/`
- 或使用 `POST /api/curves/import`
- 曲线格式参考 7.8；设备多版本与 rig 元数据参考 7.10
- 从本地 AutoEq 测量库批量导入设备频响多版本：
  ```powershell
  py tools\import_measurement_variants.py
  ```
  脚本以 `measurements/<source>/data/<form>/<device>.csv` 的 `frequency,raw` 列生成
  `curves/devices/<device>-<source>.json`，rig 元数据硬编码自各源 `name_index.tsv` 的 rig 列；
  新增来源时在脚本 `DEVICES` / `RIG_META` 中补一行即可。

### 11.6 设备频响多版本：测量 rig 策略（重要）

同一耳机的“原始频响”因测量 rig（人工耳/人头 + 补偿）不同而不同，本库把每个 rig 版本作为
独立设备曲线列出。约定与规则：

1. **默认版本 = oratory1990（`rig: GRAS 45BC-10`，KEMAR HATS）**，曲线 JSON 的 `default: true`、
   前端下拉框标 `★默认`。它是 AutoEq/Harman 校准坐标系，自动拟合首选。
2. **rig 族划分**（前端 `deviceRigFamilyKey()` / `targetRigFamilyKey()`）：
   - `oratory`：GRAS 45BC-10、43AG-7、43AC（KEMAR / 711 类耳模拟器）
   - `5128`：B&K Type 5128-B HATS（RTINGS/Headphones.com 现行）
   - `hms`：HEAD acoustics HMS II.3（InnerFidelity、RTINGS 旧图）
   - `711`：IEC 60318-4 耦合器（KB501x/KB006x 人工耳廓 + 711）
3. **跨 rig 拟合警告**：当设备 rig 族 ≠ 目标 rig 族时，自动拟合页显示黄色警告；
   中高频段不同 rig 差异最大，跨族的差值不可直接换算。目标曲线里已有 rig 专属版本
   （如 `HMS II.3 Harman over-ear 2018`、`Diffuse Field 5128`、`crinacle EARS + 711 Harman over-ear 2018`），
   应优先选与设备同族的版本。
4. **Headphone.com rig**：Headphones.com 实验室现行使用 B&K 5128 + 自研 DF HRTF 基线；
   旧 HeadRoom 时代的数据在 AutoEq 中名为 `Headphone.com Legacy`（老式夹具）。两种都暂无本库
   DT 900 Pro X / ATH-R70X 的数值数据，故未出现在下拉框；策略说明已在页面 `测量 rig 策略标准` 面板内置。
5. 重新生成数据时**只从 AutoEq 本地库取数**（离线可复现），不要手动改 points 与 rig 字段不一致。

### 11.3 扩展 AutoEq 本地库

- 更新或挂载新的 AutoEq 源码目录
- 通过环境变量 `AUTOEQ_ROOT` 指向新根目录
- `autoeq_store.py` 会在首次搜索时重建索引

### 11.4 新增前端曲线

1. `index.html` 增加图例 checkbox
2. `app.js` 的 `renderChart()` 读取该 checkbox
3. 调用 `drawLine()` 绘制曲线

### 11.5 新增后端 API

1. `mixer_web.py` 添加路由
2. 在 `_handle_api_get` / `_handle_api_post` / `_handle_api_put` 中注册
3. 前端 `api()` 直接调用

---

## 12. 不变量

1. 默认只监听 `127.0.0.1:9100`（测试可临时用 `--port` 覆盖）
2. 不访问 Reaper 8080
3. 不切换系统默认输出设备
4. 不使用 OutProcVSTPlugin 挂载 Headphone Lab
5. 所有 APO 写入必须原子替换
6. 预设内容只能由后端写；外部程序只做激活/直通
7. 激活态以 `hl-active.txt` 内容哈希对账为准
8. 服务重启（`/api/admin/restart`）只允许在非请求线程延迟关闭旧进程，且新进程必须独立进程（detached），
   日志继续写入 `peq-webui.log` / `peq-webui.err.log`；`main()` 先获取/等待单例锁再带端口绑定重试，交接期不因端口占用崩溃，也不允许第二个后端实例同时运行
