# PEQ WebUI 用户操作手册（当前版本）

> 适用：APO 配置已迁移到 `peq-webui\apo-config` 后的版本
> 日常使用不再需要 UAC / 管理员权限
>
> 如果页面显示“配置未托管 / 未接管”，请先参考文末“首次接管”。

---

## 1. 启动

### 方式一：双击启动

```text
F:\MIMO-Space\PEQ-WebUI\启动PEQ-WebUI.cmd
```

### 方式二：命令行启动

```powershell
cd F:\MIMO-Space\PEQ-WebUI
.\start-webui.ps1
```

或：

```powershell
py mixer_web.py --port 9100
```

启动后打开：

```text
http://127.0.0.1:9100/
```

当前版本 `start-webui.ps1` 以普通权限后台启动，不需要 UAC。  
只有首次把 APO 配置从 `C:\Program Files\EqualizerAPO\config` 迁移到项目内目录时，才需要一次管理员操作。

---

## 2. 页面说明

顶部状态显示：

- APO 已安装 / 未安装
- 配置已托管 / 未接管
- FiiO 已挂载 / 未检测到挂载
- 管理员模式 / 普通模式
- 当前激活预设 / 直通

页面右上角提供：

- **❓ 帮助**：点击后快速调出《用户操作手册》，无需离开当前页面。
- **⏻ 重启服务**：停止当前后端进程并自动拉起新进程。页面会短暂断开（约 3~5 秒），服务恢复后自动刷新页面。适合代码/曲线更新后刷新后端状态。
- **⏹ 停止服务**：停止当前后端进程。页面随即断开并显示“服务已停止”提示（内含重新启动命令）。停止后需在本机手动启动：
  ```text
  cd F:\MIMO-Space\PEQ-WebUI
  .\start-webui.ps1
  ```
  或双击 `启动PEQ-WebUI.cmd`，然后刷新页面。服务开关在离线状态下不可用。

常用区域：

- 预设卡片：单击选中，双击激活试听；可重命名、删除、拖拽调整顺序（顺序会保存到后端）；`pure`（直通）固定置顶；新建或改绑设备后自动放入同设备预设块
- 曲线图：默认显示 Raw 原始、Target 目标、Equalizer 均衡器、Equalized 均衡后；已保存 / 草稿 / 预览默认关闭，可手动打开
  - 在曲线上拖动 Band 节点可改频点和增益；滚轮调 Q；双击空白处添加 PK 频点
  - 曲线图右上角支持**显示归一化**（1kHz / 500Hz-2kHz 平均 / 100Hz-10kHz 平均）和**显示平滑**（1/24、1/12、1/6 oct），只影响视觉，不影响拟合结果
  - 勾选**残差**后可显示 `Equalized - Target` 或 `Raw - Target` 误差曲线，帮助判断哪里没拟合好
- 频段参考：频响图下方的 Sub Bass / Mid Bass 等频段条可直接点击，点击后显示该频段介绍并在频响曲线上高亮，详细调整建议见第 12 章
- EQ 表格：直接修改启用 / 类型 / 频点 / 增益 / Q，行尾可删除
- 手动滑块：频率 / 增益 / Q，可拖动或直接输入数值
- 顶部目标曲线下拉框：选择 Harman / 扩散场 / 平直 / 自定义目标，选择后会同步保存到当前预设；下拉框按“音频常用目标曲线”和“耳机目标曲线”分组显示
- 自动拟合开关：打开后选择/更换目标曲线或设备时，会自动拟合并采用为草稿
- 导入曲线按钮：手动粘贴 `freq,db` 或 GraphicEQ 文本，新增设备/目标曲线
- 工具页签：`导入EQ` / `自动拟合` / `AutoEq 库`
- EQ 风险提示：底部实时显示当前增益是否偏高，并建议 Preamp
- 底部：保存到预设 / 保存后立即激活 / 放弃草稿 / 撤销上次保存

---

## 2.1 绑定设备：频响版本与测量 rig 策略

同一个耳机型号的“原始频响”在不同**测量装置（rig）**下测得的结果并不相同（差异主要来自人工耳/人头模型与
频率补偿策略）。页面把每个 rig 版本作为独立条目列出，**下拉框分组显示**，选项格式：

```text
设备名 · 数据源 / rig 短标签 [★默认]
例如：铁三角 ATH-R70X · RTINGS / HMS II.3
```

- **默认版本（★默认）= oratory1990 / GRAS 45BC-10（KEMAR 人头）**：与 Harman 目标同源，校准首选。
- 切换设备版本后，曲线图、自动拟合都以该版本为基准；绑定会随预设保存。
- 把鼠标悬停在选项或顶部“当前频响”信息条上，可查看该 rig 的标准说明与来源链接。
- 页面顶部有 **“测量 rig 策略标准 ⓘ”** 可折叠面板，列出各类 rig（oratory 基准 / 5128 / 711 /
  HMS II.3 / Headphone.com rig）与本库收录情况。
- **跨 rig 拟合提醒**：当设备 rig 与目标曲线 rig 不同族（例如设备是 711 类、目标是 Harman 头戴 2018），
  自动拟合面板会显示黄色警告。高频段不同 rig 差异最大，跨族结果只可作参考；建议优先选与设备同族的目标
  （如 `HMS II.3 Harman over-ear 2018`、`Diffuse Field 5128`、`crinacle EARS + 711 Harman over-ear 2018`）。

> 详细版本矩阵与维护方式见《开发维护文档.md》§7.10 / §11.6。

---

## 3. 新建预设

1. 点击 `+ 新增预设`
2. 输入名称
3. 选择绑定设备（可先不选）
4. 点击创建

创建后会进入空预设，默认 Preamp 为 -6 dB，无 EQ 频段。

---

## 4. 添加 EQ 频点

### 方法一：按钮添加

点击：

```text
+ 添加频段
```

### 方法二：图表双击/拖拽

- 在曲线空白处双击，会在该位置添加一个 PK 频点
- 拖动已有点：横轴改变频率，纵轴改变增益
- 在点上滚动滚轮：调整 Q 值

### 方法三：导入 EQ

在“导入EQ”页粘贴 EqualizerAPO / AutoEq / CSV / JSON 文本，点击“解析”，确认紫色预览后“采用为草稿”。

### 方法四：自动拟合

1. 顶部或“自动拟合”页选择目标曲线，例如 `Harman 头戴 2018`
2. 可以打开 `自动拟合` 开关，让系统自动拟合并直接填入草稿
3. 也可以在“自动拟合”页设置滤波器数、频带范围、最大增益、最大 Q，手动点击“拟合预览”，再“采用为草稿”

### 方法五：AutoEq 本地库导入

在“AutoEq 库”页搜索耳机名称，例如 `AKG K371`。每个结果有两个操作：

- `导入原始频响为目标`：读取 AutoEq 结果里的 `raw` 原始频响，保存成“目标曲线”；适合把另一副耳机（例如 R70XA）作为拟合目标。若开启了自动拟合，还会立即触发拟合。**导入后，“目标曲线”下拉框中该条目会显示测试设备信息**，格式为 `设备名 · 测试源 / rig`（如 `Sennheiser HD 800 (原始频响) · oratory1990 / GRAS 45BC-10`），悬停可看完整的 rig 标准说明与来源链接。
- `设备+PEQ`：导入 AutoEq 的原始频响曲线（`raw` 列）和预计算 PEQ，写入当前预设（无当前预设时自动新建），并把该设备的 AutoEq 目标一并记入预设。导入的设备条目同样带 rig 标签，与手写/拟合的设备版本并列显示。

### 方法六：手动导入设备/目标曲线

点击页面上的 `导入曲线`：

1. 选择“设备原始频响”或“目标曲线”
2. 填写名称
3. 粘贴 `freq,db`、`freq<tab>db`、CSV 或 `GraphicEQ: ...` 文本
4. 点击“导入”

同名曲线会覆盖。导入后刷新设备/目标下拉框，无需重启后端。

---

## 5. 测试

测试就是“试听当前效果”：

1. 在预设卡片中单击选中预设（只选中不发声）
2. **双击**该预设卡片，会激活它并写入 APO
3. 激活后，当前预设的 EQ 会写入：

```text
F:\MIMO-Space\PEQ-WebUI\apo-config\hl-active.txt
```

APO 检测到文件变化后立即生效。

---

## 6. 保存

### 保存到预设

点击底部：

```text
保存到预设
```

这会写入：

```text
F:\MIMO-Space\PEQ-WebUI\apo-presets\<预设id>.txt
```

### 保存并立即激活

手动勾选底部（默认关闭）：

```text
保存后立即激活
```

点击“保存到预设”，会同时：

- 写入 `apo-presets\<id>.txt`
- 复制到 `apo-config\hl-active.txt`
- APO 实时生效

### 不自动保存

当前版本**不会自动保存**。无论编辑的是不是激活中的预设，修改都只停留在草稿状态；需要点击底部“保存到预设”或勾选“保存后立即激活”后才会写入预设/APO。

### 切换预设时的保存提示

如果当前预设还有未保存草稿，切换预设会弹出“保存 / 不保存 / 取消”。选择“保存并切换”会先保存当前预设再切换。

---

## 7. 持久化到 APO

“持久化”分两层：

```text
预设文件：
apo-presets\<id>.txt        ← 长期保存，重启不丢

激活文件：
apo-config\hl-active.txt    ← APO 当前正在加载的内容
```

所以：

- 只保存：预设文件更新，但当前声音不一定变
- 保存并激活 / 双击预设激活：预设文件 + `hl-active.txt` 都更新，当前声音立即变

如果之后不开 WebUI，外部中控程序可以直接：

```text
读 apo-presets\<id>.txt
→ 写入 apo-config\hl-active.txt
→ APO 生效
```

全程不需要 WebUI、UAC、管理员权限。

---

## 8. 直通

在页面激活直通预设 `pure` 即可。

WebUI 侧对应：

```text
激活 null / pure
→ apo-config\hl-active.txt 写为仅注释/空内容
→ APO 直通
```

---

## 9. 常用路径

| 内容 | 路径 |
|---|---|
| WebUI 项目 | `F:\MIMO-Space\PEQ-WebUI\` |
| 预设文件 | `apo-presets\*.txt` |
| 预设清单 | `apo-presets\presets.json` |
| APO 激活文件 | `apo-config\hl-active.txt` |
| APO 总配置 | `apo-config\config.txt` |
| 设备曲线 | `curves\devices\*.json` |
| 目标曲线 | `curves\targets\*.json` |
| AutoEq 本地源码库 | 默认 `F:\MIMO-Space\AutoEq-4.1.2\AutoEq-4.1.2`，可用环境变量 `AUTOEQ_ROOT` 覆盖 |

---

## 10. 首次接管（仅未迁移/未托管时需要）

如果顶部显示“配置未托管 / 未接管”，说明 APO 仍在读取 Program Files 下的原配置，尚未迁移到项目内目录。此时需要一次管理员操作：

打开“Windows PowerShell（管理员）”（可右键开始按钮选择），然后执行：

```powershell
cd F:\MIMO-Space\PEQ-WebUI
py -c "from apo_backend import ApoBackend; ApoBackend().auto_install()"
```

完成后再次打开 WebUI，状态应变为“配置已托管”。之后日常使用不再需要管理员权限。---

## 11. 自动拟合算法：怎么选、怎么用

### 11.1 自动拟合在做什么

自动拟合的目标是：**找到一组 EQ 参数，让你当前设备的“原始频响曲线”尽量变成你选的“目标曲线”。**

例如：

```text
设备：拜亚动力 DT 900 Pro X
目标：Harman 头戴 2018
```

WebUI 会计算两者的差距，然后生成若干个 EQ 频点（PK），并自动给你一个 Preamp。

---

### 11.2 快速算法（纯 numpy）

快速算法可以这样理解：

1. 先看“哪里差得最多”；
2. 在那个地方放一个山峰/山谷形滤波器；
3. 重新计算剩余误差；
4. 再找下一个差得最多的地方；
5. 最后把每个滤波器的频率、Q、增益反复微调，直到残差不再明显下降。

特点：

- 快：通常 <1 秒
- 纯 RMS 拟合度通常更好
- 不依赖 scipy
- 缺点：它追求“曲线贴得最紧”，有时候会用比较尖的高 Q 峰去压数值，听感可能偏锐

适合场景：

- 最看中“曲线是否贴合”
- 目标曲线本身有明确的高频峰、特殊音色
- 想快速得到一版 EQ 再手动微调

---

### 11.3 精确算法（AutoEq SLSQP）

精确算法是 AutoEq 官方的优化器，可以这样理解：

- 它把所有滤波器的频率、Q、增益看作一组“旋钮”
- 不是一个个轮流调，而是**所有旋钮一起联合微调**
- 优化过程中会加入“安全惩罚”：如果某个峰太陡、Q 太高、增益太大，就会惩罚
- 10kHz 以上默认只比较“平均能量”，不强行追 10kHz 后的小峰

特点：

- 更稳、更保守，不容易出现高 Q 对消对
- 听感上通常更“耐听”，但纯 RMS 不一定比快速低
- 需要 scipy；未安装时页面会提示并自动回退快速

适合场景：

- 想要更稳定的、适合长期听的 EQ
- 目标是 Harman / 5128 / 扩散场这类“听感目标”
- 希望开启高低架（LS/HS）
- 愿意接受 RMS 可能略高 0.1~0.4dB

---

### 11.4 两者局限性

无论快速还是精确，都不能完全消除误差：

- 不同测量 rig 之间本身有系统差异，跨 rig 拟合天然做不到“完美”
- 耳机单元个体差异、佩戴方式、耳罩老化都会让曲线变化
- 10kHz 以上人耳差异大，拟合“纸面曲线”不代表实际听感一定好
- 滤波器数量有限，不可能无限逼近
- 自动拟合结果是“起点”，不是“终点”

---

### 11.5 如何选择拟合算法

| 场景 | 建议 |
|---|---|
| 目标是另一副耳机原始频响，想“复刻”它的特色 | 快速 |
| 目标是 Harman / 5128 / 扩散场 / 听感目标 | 精确更稳；也可快速直出 |
| 目标在 10kHz 后有明确波峰 | 快速；或精确 + 勾选“高频逐点” |
| 开启高低架（LS/HS） | 精确（页面会自动切到精确） |
| 想快速试听一版再手动改 | 快速 |
| 想长期保存、多设备复现 | 精确 |

---

### 11.6 高低架（LS/HS）是什么？要不要开？

- **PK（Peaking）**：只在某个频率附近抬升/衰减，像一个“山峰/山谷”
- **LS（Low Shelf）**：低频整体抬升/衰减，像一个“斜坡”，常用于低频量感
- **HS（High Shelf）**：高频整体抬升/衰减，常用于整体亮度/齿音

什么时候可以考虑开高低架：

- 你觉得“整体低频太多/太少”，而不是某个低频峰太突出
- 你觉得“高频整体偏暗/偏亮”，而不是某个 8kHz 峰太尖

什么时候不建议开：

- 你只想要曲线拟合最准
- 10 段预算本来就不够，再拿 1~2 段给 shelf，窄峰拟合会变差
- 使用快速引擎时高低架容易退化，所以页面在“开启高低架”时会强制用精确引擎

结论：**默认不需要开高低架，先自动拟合试听；如果感觉整体音色倾斜不对，再手动加一条 LS/HS，或打开高低架重新用精确拟合。**

---

### 11.7 拟合后如何继续优化成自己的听感

1. **先别急着保存**，直接激活试听当前拟合结果。
2. 记录你觉得不舒服的地方，比如：
   - 低频太多/太少
   - 人声太远/太近
   - 高频太刺/太暗
3. 在曲线图上直接拖拽频点：
   - 横向拖 = 改频率
   - 纵向拖 = 改增益
   - 滚轮 = 改 Q
4. 在右侧 EQ 表格精确改参数：
   - 想“整体亮一点”：加一条 PK 8~10kHz，增益 +1~2，Q 0.7~1.2
   - 想“人声近一点”：在 2~4kHz 加少量增益
   - 想“低频少一点”：在 100~200Hz 减 1~2dB，Q 0.7~1
5. 每改一次就听一下，页面会更新预览，但不会自动保存；
6. 最终确认后再点击“保存到预设”或“保存后立即激活”。

小技巧：

- Q 越小，影响范围越宽，越像“整体音色调整”
- Q 越大，影响范围越窄，越适合修“某个峰/谷”
- 增益一次不要加超过 3~4dB，先用小步进试
- Preamp 由系统自动计算，手动大增益会造成削波风险

---

### 11.8 常见疑问

**Q：精确引擎提示 scipy 不可用怎么办？**
安装 scipy 后重启 WebUI：

```powershell
py -m pip install scipy
```

如果无法安装，继续用快速引擎即可。

**Q：为什么精确拟合的 RMS 比快速高？**
因为精确算法带有防振铃/防尖锐峰的安全惩罚，它不是在单纯压 RMS，而是在“稳定”和“拟合度”之间取舍。

**Q：10kHz 以后拟合不准正常吗？**
精确算法默认 10kHz 以上只对比平均能量，所以 10kHz 后的峰形拟合不过快速。需要追 10kHz 后峰形时，可以用快速，或在精确模式下勾选“高频逐点”。

**Q：开启高低架后低频出现很大提升正常吗？**
不正常。那通常是高低架 + PK 堆叠退化的表现。当前版本已做保护：高低架候选如果没有比纯 PK 更好，会自动关闭高低架并提示。
---

### 11.9 高频逐点开关（精确引擎）怎么选

AutoEq 官方的策略是：**10kHz 以上只比较“平均能量”**，不追峰形。  
勾选“高频逐点”后，精确引擎会：

- 允许 PK 放到 10kHz 以上（到你设置的频带上限）
- 10kHz 以上不再平均化，而是逐点拟合
- 去掉“防尖锐峰”惩罚，让优化器敢放高 Q 段

#### 10kHz 以上的峰值到底怎么影响听感

- 10k~20kHz 是“空气感、齿音、高频细节”所在区域
- 大部分成年人（尤其 25 岁后）对 10k 以上敏感度明显下降
- 同一个耳机在不同佩戴、不同耳道、不同测量 rig 下，10k 以上的曲线差异非常大
- 所以 10k 后的峰：

  | 峰的类型 | 听感 | 风险 |
  |---|---|---|
  | 宽峰（Q 低） | 整体偏亮/偏暗，影响“高频量感” | 低 |
  | 窄峰（Q 高） | 局部刺耳、齿音重、金属声 | 高 |
  | 测量毛刺 | 拟合它=拟合噪声 | 高 |

简单说：**10k 后的宽特征可信，窄尖峰要警惕。**

#### 推荐开启的情况

- 目标是另一副耳机原始频响，它 10k 后有**明显的大峰/大谷**且你想复刻（如 MSR7b 那种 10k 峰）
- 快速拟合的 10k 后形状你觉得对，但想用精确引擎当基础再微调

#### 不推荐开启的情况

- 目标是 Harman / 5128 / 扩散场这类听感目标（本身没有毛刺峰）
- 日常调音：10k 后拟得太准反而容易刺耳
- 段数本来就紧张（<10 段），把预算留给 100Hz~8kHz 更重要
- 目标曲线高频部分有明显测量毛刺

#### 一个重要提醒

勾选“高频逐点”**只代表允许优化器在 10k 后放段**，并不代表一定会放。  
AutoEq 的布点仍由“全局最大峰”决定，如果 10k 后的峰不够大，优化器不会自动挪过去。

如果你想**强制**让 10k 后贴合，最直接的办法是：

1. 用快速引擎；或
2. 精确拟合后手动在 10k~12k 加一条 PK 微调
---

### 11.10 快速引擎的三个可选优化

WebUI 的“自动拟合”页提供三个额外选项，默认都关闭，适合有明确需求时手动打开：

#### 1. 频带上限超过 15kHz 警告

- 当“频带上限 (Hz)”设置为 **15000 以上**时，页面会显示警告：
  > 15kHz 以上 RBJ 曲线失真明显，拟合结果可能与实际听感不符。

- 原因：在 48kHz 采样下，15kHz 以上的 RBJ 滤波器形状开始压缩失真。
- 建议：日常使用把频带上限设在 **12000Hz** 以内；只有当你有明确的高频目标且知道风险时才开到 15000Hz 以上。

#### 2. 平滑（1/12 oct）

- 勾选“平滑（1/12 oct）”后，快速引擎在拟合前会对需求曲线做 1/12 倍频程高斯平滑。
- 作用：抹掉测量毛刺，减少“为了拟合噪声而生成的高 Q 窄峰”。
- 适合：
  - 目标是 Harman / 5128 / 扩散场等听感目标
  - 目标曲线高频有明显毛刺
- 不适合：
  - 想精确复刻另一副耳机的原始频响/签名
  - 拟合目标本身就是“测量曲线上的尖峰细节”
- 注意：开启后界面显示的 RMS 通常会下降，但这是“平滑后的拟合度”，不代表实际听感一定更好。

#### 3. 锐度惩罚（快速）

- 勾选“锐度惩罚（快速）”后，快速引擎在优化时会额外惩罚高增益 + 高 Q 的窄峰。
- 作用：减少齿音、金属声、过尖的高频峰；让 EQ 更温和。
- 代价：纯 RMS 会变差（比如从 0.29 升到 0.55），因为它在“拟合数值”和“听感安全”之间主动选择了后者。
- 适合：
  - 快速拟合后听起来刺耳、齿音重
  - 想要更耐听、更保守的快速结果
- 不适合：
  - 你希望快速引擎始终保持最低 RMS/最高贴合度

#### 推荐使用组合

| 目的 | 建议 |
|---|---|
| 曲线最贴合 | 全部关闭 |
| 更顺滑、更耐听 | 开“平滑” + 开“锐度惩罚” |
| 复刻另一副耳机 | 只关“平滑”，可尝试“锐度惩罚” |
| 高频细节优先 | 只关“平滑”，关“锐度惩罚”，频带上限不超过 12k |

这些选项只影响快速引擎；精确引擎继续使用 AutoEq 官方逻辑。

### 11.11 目标曲线快速整形

在“自动拟合”页的“目标曲线快速整形”区域，可以在拟合前调整目标曲线：

- **EQ Amount (0-1)**：在原始设备曲线和目标曲线之间插值。`1` = 完整目标，`0.8` = 只拟合 80% 的目标差异，`0` = 保持原设备。
- **Bass (dB)**：对低频整体增减，正数增加低频。
- **Treble (dB)**：对高频整体增减，负数减少高频/齿音。
- **Tilt (dB/oct)**：以 1kHz 为参考的每倍频程倾斜，正值整体高频更多。

调整后的目标只用于本次拟合，不会覆盖原始目标曲线。拟合后如果觉得方向对，可以手动微调 EQ，或把该目标另存为自定义目标曲线。

### 11.12 自动滤波器数选择

勾选“自动段数”后，后端会依次尝试 `5/10/15/20` 段（不超过你设置的最大段数），并返回每个段数对应的 RMS、加权 RMS 和 Preamp。

- 默认选择“满足目标 RMS 的最小段数”；
- 如果所有段数都达不到目标 RMS，会自动返回最优结果并提示；
- 在结果区可以点击某个段数按钮，直接采用该段数的拟合结果。

适合不知道选 5 段还是 20 段的用户。

### 11.13 感知/听感加权拟合

“感知加权”下拉框提供：

- **关闭**：全频段等权，追求最低 RMS；
- **温和**：200Hz-8kHz 权重略高，10kHz 以上降低；
- **中频优先**：500Hz-4kHz 权重最高，适合人声/中频优先的调音；
- **高频保守**：10kHz 以上权重明显降低，减少对高频毛刺的过度拟合。

开启后：

- 优化器会优先照顾人耳更敏感的中频；
- 结果会同时显示“RMS”和“加权 RMS”，方便对比；
- 配合“残差曲线”可以直观看到加权后哪里被优先拟合。

注意：感知加权通常会牺牲一点未加权 RMS，但听感可能更耐听。

---

## 12. 频段参考：常见影响乐器与调整建议

> 频响图下方的“频段参考”块是速查版；下表给出更详细的调整建议。

### 12.1 频段速查表

| 频段 | 名称 | 常见影响乐器 | 调整建议 |
|---|---|---|---|
| 100Hz 以下 | 极低频 | 底鼓、大鼓、贝斯、管风琴、合成器Bass | **低频下潜**。提升可增加冲击力、氛围和身体感；过量容易浑浊、轰头，建议用小增益宽 Q（LS/PK）微调。 |
| 100-250Hz | 低频 | 男声低音、大提琴、贝斯、钢琴低音、鼓腔 | **人声厚度**。提升让声音更温暖厚实，过量会发闷/轰头；觉得人声薄可在此 +1~3dB，Q 0.7~1.2。 |
| 250-600Hz | 中低频 | 人声胸腔、木吉他、钢琴中低音、大提琴、军鼓 | **人声与器乐饱满度**。降低可提高清晰度，但降太多人声会发虚、缺少力度；想增加饱满度可在此微增。 |
| 600-1000Hz | 中频 | 人声、中提琴、萨克斯、电吉他、钢琴中音、军鼓 | 少了人声有点虚，多了会出“收音机声”，**不建议大幅调整**；如确有鼻音/盒感可在中频窄 Q 衰减。 |
| 1000-2000Hz | 中高频 | 人声、小提琴、长笛、吉他、钢琴高音、镲片 | **清晰度、声场**。提升让人声更近、有头中效应；少了人声远、虚、塑料感。调整时从 ±1~2dB 开始。 |
| 2000-5000Hz | 高频 | 小提琴、木管、铜管、吉他泛音、人声辅音、镲片 | **器乐清晰度/锐利感**。多了高频碎、毛刺感；少了缺律动但更柔和。人声“近/亮”也主要看此段。 |
| 5k-10kHz | 高频 | 镲片、三角铁、沙锤、打击乐泛音、人声齿音 | **清晰度、齿音**。提升更亮、更多空气细节；过量齿音（s/t）刺耳。齿音重可在此段窄 Q 衰减。 |
| 10k-20kHz | 极高频 | 镲片泛音、三角铁、空气声、弦乐泛音、金属打击乐 | **泛音、空气感、通透度**。提升更开放、通透、明亮；过量金属声、刺耳。因人耳差异大，建议小幅试听。 |

### 12.2 使用建议

- 先听出问题，再对照频段定位，不要“每个频段都动”。
- 一般先处理 250-600Hz 和 1000-2000Hz，它们对人声和清晰度影响最直接。
- 低频/极低频用 LS 或低 Q PK 更自然；高频/极高频用 HS 或中等 Q PK，避免过窄尖峰。
- 每次调整幅度建议先从 ±1~2dB 开始，试听后再逐步加/减。
- 如果只是“想让声音亮一点”，优先 5k-10kHz 或 10k-20kHz 微增；如果刺耳，优先 2k-5kHz 或 5k-10kHz 窄 Q 衰减。
- 自动拟合结果同样可以用本表判断“哪个频点是在补厚度/清晰度/齿音”，再决定是否保留。

---

## 13. 常见目标曲线听感介绍

> 目标曲线不是“标准答案”，而是不同机构/社区对“好听、均衡、监听”的不同定义。下面介绍常见曲线的大致听感方向，帮助你判断“拟合到某条曲线后到底会得到什么声音”。

### 13.1 哈曼曲线（Harman）

- 来源：Harman International 基于大量盲听偏好研究得出的目标曲线，分头戴（over-ear）和入耳（in-ear）两套。
- 听感特点：
  - 低频比平直/扩散场更饱满，有温暖、有力、氛围感；
  - 中频自然，人声位置适中；
  - 高频有适度能量，整体不容易刺耳；
  - 适合流行、电子、摇滚、电影等需要低频冲击和氛围感的听音。
- 注意：不是“动词打次”曲线，低频不会过量到轰头；如果耳机本身低频多，仍可能觉得偏多。

### 13.2 扩散场（Diffuse Field / DF）

- 来源：基于扩散声场/房间均衡的补偿曲线，常见于监听与声学测量领域。
- 听感特点：
  - 中高频相对更亮、更“监听”；
  - 低频通常比哈曼少，声音更干净、冷静；
  - 人声和乐器定位更清晰，适合混音、监听、分析录音；
  - 日常欣赏可能觉得偏薄、偏亮、缺少氛围。
- 适用：喜欢“白开水、揭示录音原貌”的用户。

### 13.3 水月雨目标（Moondrop Target / VDSF）

- 来源：水月雨提出的 Virtual Diffuse Sound Field（VDSF）目标，结合扩散场与入耳声学特性。
- 听感特点：
  - 介于哈曼与扩散场之间，低频比扩散场多、比哈曼收敛；
  - 中高频自然，人声清晰且有适当距离感；
  - 高频平滑，追求自然、宽松、耐听；
  - 适合喜欢均衡、清新、人声/小编制的用户。
- 注意：不同时期水月雨目标有 VDSF 1/2 等版本，曲线细节略有差异。

### 13.4 JM-1（JM1）

- 来源：社区测量与听感研究提出的入耳式目标曲线。
- 听感特点：
  - 低频略少于哈曼，但比扩散场更有力度；
  - 中频人声近、情感丰富；
  - 高频抑制部分刺耳峰，整体较顺滑；
  - 常被评价为“人声毒、耐听、不刺激”。
- 适用：流行人声、ACG、小编制、长时间聆听。

### 13.5 其他常见目标曲线速览

| 目标 | 一句话听感 |
|---|---|
| IEF Neutral | 接近中性的入耳目标，比哈曼低频少、比扩散场温和，适合录音监听 |
| HBB | 低频较足、中频厚实、高频顺滑，流行/摇滚氛围向 |
| Crinacle | 强调中高频清晰度与自然度，低频适中，适合“通透、清晰”走向 |
| 平直（Flat） | 追求测量频响平直，听感往往偏亮、偏薄，不一定是日常“好听” |
| 某耳机原始频响 | 复刻该耳机的音色，适合“想要另一副耳机的味道”场景 |

### 13.6 怎么选

- 不想动脑、追求大众好听：**哈曼**
- 监听、混音、看频响：**扩散场 / IEF Neutral**
- 喜欢清新耐听、人声小编制：**水月雨 VDSF**
- 喜欢人声近、不刺激：**JM-1**
- 喜欢低频氛围：**HBB / 哈曼**
- 最终请以自己的耳朵为准，目标曲线只是起点。
